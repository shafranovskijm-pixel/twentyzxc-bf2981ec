

## Plan: Переделка планера в календарь + исправление темы + настраиваемый баннер/фон

### 1. Переделать Планер — месячный календарь (как на картинках)

**`src/components/admin/PlannerTab.tsx`** — полная переработка:

```text
┌────────────────────────────────────────────────┐
│  < Апрель 2026 >   [Сегодня]  [Неделя|Месяц]  │
├──────┬──────┬──────┬──────┬──────┬──────┬──────┤
│  Пн  │  Вт  │  Ср  │  Чт  │  Пт  │  Сб  │  Вс │
├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│      │      │  1   │  2   │  3   │  4   │  5   │
│      │      │ task │      │      │      │      │
├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│  6   │  7   │  8   │  ...                      │
└──────┴──────┴──────┴──────────────────────────────┘
```

- **Два режима**: Неделя (текущий, сохранить) и Месяц (новый, по умолчанию)
- Месячная сетка: 6 рядов × 7 столбцов, ячейки с задачами-точками/карточками
- Сегодняшний день выделен золотым акцентом
- Клик на день — раскрывает попап/панель с задачами дня и кнопкой "Добавить"
- Задачи отображаются как цветные мини-бейджи внутри ячеек (цвет по статусу)
- DND перетаскивание задач между днями сохраняется
- Фильтры по статусу и клиенту остаются
- Мини-календарь слева (как на второй картинке) — опционально, если будет место

### 2. Исправить переключение темы

**`src/pages/Admin.tsx`**:
- Добавить `useState` для `isDark` чтобы React перерисовывал иконки Sun/Moon
- При клике: `setIsDark(!isDark)` + `classList.toggle("dark")` + `localStorage`
- Инициализация из `localStorage` в `useState`

### 3. Настраиваемый баннер (картинка)

**`src/pages/Admin.tsx`**:
- Баннер: если в `localStorage` есть `admin-banner-url` — показывать `<img>`, иначе градиент
- Кнопка-оверлей "📷 Изменить" при наведении на баннер
- При клике — `<input type="file">` → загрузка в Supabase Storage (bucket `admin-assets`) → сохранение URL
- Кнопка сброса баннера к градиенту

### 4. Настраиваемый фон

**`src/pages/Admin.tsx`**:
- В дропдауне настроек добавить пункт "Оформление"
- Новая секция: выбор фона (несколько пресетов градиентов + загрузка своего фона)
- Сохранение в `localStorage` ключ `admin-bg-style`

### Миграции
- Создать Storage bucket `admin-assets` (если не существует) для загрузки баннеров

### Файлы
- **Переписать**: `src/components/admin/PlannerTab.tsx` — месячный календарь с переключением неделя/месяц
- **Редактировать**: `src/pages/Admin.tsx` — исправить тему, добавить загрузку баннера, настройки фона

