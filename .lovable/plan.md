

## Plan: Настройки сайта — вертикальные табы + убрать из сайдбара

### Что делаем

1. **Убрать «Настройки сайта» из сайдбара** (`AdminSidebar.tsx`): удалить `{ id: "site-settings", ... }` из `defaultMenuItems`

2. **Перенести настройки сайта в Профиль** (`Admin.tsx`):
   - В секции `profile` добавить все табы настроек сайта (SEO, Контакты, Акции, Реквизиты, История, НМО, ФРДО) как **вертикальные табы** слева
   - Структура: слева — вертикальный список табов (Оформление, Тема, Баннер, Тариф + все настройки сайта), справа — контент выбранного таба
   - Убрать отдельную секцию `site-settings` — всё живёт в `profile`

3. **Вертикальная компоновка** (как на скриншоте — табы столбиком слева):
```text
┌──────────────┬─────────────────────────┐
│ Оформление   │                         │
│ SEO          │   [контент активного    │
│ Контакты     │    таба]                │
│ Акции        │                         │
│ Реквизиты    │                         │
│ История      │                         │
│ НМО Портал   │                         │
│ ФИС ФРДО    │                         │
│ Тариф        │                         │
└──────────────┴─────────────────────────┘
```

### Файлы
- **`src/components/admin/AdminSidebar.tsx`** — удалить `site-settings` из `defaultMenuItems`
- **`src/pages/Admin.tsx`** — объединить profile + site-settings в одну секцию с вертикальными табами, удалить `activeSection === "site-settings"` блок

