

## Plan: Реструктуризация админ-панели — хедер наверх, настройки сайта в сайдбар, профиль как секция

### 1. Хедер ВЫШЕ баннера

**`src/pages/Admin.tsx`**: Поменять местами `<header>` и баннер — хедер идёт первым, баннер под ним.

### 2. Хедер: логотип + статус + тариф

В хедере слева:
- Лого «Σ» + «СИНТАГМА» + подпись «Администратор» (как на image-315)
- Заглушка «Тариф» (Badge) — кликабельная, пока без функционала

Справа: уведомления, тема, «Профиль» (ведёт на секцию профиля)

### 3. Сайдбар: добавить «Настройки сайта»

**`src/components/admin/AdminSidebar.tsx`**: Добавить пункт `{ id: "site-settings", label: "Настройки сайта", icon: Settings }` в `defaultMenuItems`. Убрать `secondaryItems` из дропдауна в `Admin.tsx`.

### 4. Секция «Настройки сайта» — отдельная вкладка

**`src/pages/Admin.tsx`**: При `activeSection === "site-settings"` показывать вкладки-табы внутри: SEO, Контакты, Акции, Реквизиты, История, НМО, ФИС ФРДО. Всё то, что раньше было в дропдауне Settings ⚙️, теперь открывается как полноценная секция в центре.

### 5. Секция «Профиль» — отдельная вкладка

**`src/pages/Admin.tsx`**: Новая секция `activeSection === "profile"`:
- Переключение темы (светлая/тёмная)
- Выбор фона (пресеты + кастомный)
- Загрузка баннера (перенести из баннер-оверлея)
- Заглушка «Тариф: Бесплатный» (для будущей настройки)

Убрать дропдаун Settings ⚙️ из хедера — всё переезжает в эти две секции.

### Файлы
- **`src/components/admin/AdminSidebar.tsx`** — добавить «Настройки сайта» + «Фон» пресеты внизу сайдбара (как на image-316)
- **`src/pages/Admin.tsx`** — хедер выше баннера, логотип+статус+тариф, новые секции site-settings и profile, убрать дропдаун

